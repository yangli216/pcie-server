# floating-ball-server 需求基线

## 两慢病插件一期

一期为 `floating-ball` 医生桌面端提供高血压与 2 型糖尿病慢病随访持久化，不包含患者端，也不建设管理端规则配置页。

边界如下：

1. 慢病保存请求严格采用对接系统原始 `TcdVisitForm` 字段和结构；不得用平台自创的英文通用字段替换原字段。
2. 服务端接收并保存医生确认后的融合随访记录；`sdVisitKind=1/2/1,2` 分别表示高血压、糖尿病、联合随访。公卫身份由桌面端上游 `rqflStatus` 派生，页面只读，不允许医生在表单内增删病种。
3. 设备鉴权使用现有 Bearer device token 与 ECDSA P-256 请求签名，不新增绕过路径。
4. AI 不是规则发布者。AI 只生成医生确认前的健康处方草稿，不修改正式路径或历史随访记录。
5. 一期管理端无新增页面；模板与路径使用代码仓库审核清单和数据库基线发布。
6. 健康处方和年度评估打印前保存强类型快照，至少包含患者/就诊锚点、病种、数据截至时间、规则/路径版本、年度指标、医生确认项与打印医生；快照保存失败不得被客户端视为可追溯打印完成。
7. 单病种和双病种都保存一条融合记录；原实例 8 个多选数组在提交前转成逗号字符串，`drugList` 保持对象数组。稳定幂等键使用签名请求头 `X-Request-Id`，同一机构内重试不得形成重复记录。

## 部署与容量扩展边界

1. 产品默认以 `FB_DEPLOYMENT_MODE=standalone` 单节点交付。当前客户量由单机承载时，不要求额外部署 Nginx 或共享存储，也不因未来可能扩容提前拆分微服务。
2. 客户新增机构或一批用户的时间可预期。只有计划批次的 AI、SSE 或实时语音长连接峰值超过最近一次同规格单机安全容量时，才由运维在上线窗口前人工启用 `FB_DEPLOYMENT_MODE=ai-scale-out` 并增加节点；不建设自动扩缩容。
3. `ai-scale-out` 只扩展 `/v1/ai/chat`、`/v1/ai/speech/transcribe`、`/v1/ai/speech/realtime` 与 `/v1/ai/speech/realtime/ws`，不等同于全站 active-active。管理端、普通业务和其他默认路径继续由唯一主节点处理。
4. 扩展节点与主节点使用同一应用版本、同一业务数据库、数据库 nonce 和同一份共享 POSIX 发布/语音存储；每个节点必须有唯一节点标识。主节点可以同时承担 AI/长连接流量。
5. 本阶段不以普通业务高可用为目标，不把 PatientMemory、LIS/PACS、推荐偏好聚合、问诊日志、EMR 缓存等普通业务并发写路径分发到多个节点；未来确需全站多节点时另立需求和验收合同。
6. 本阶段不引入 Redis、MQ、服务发现、微服务拆分或自动扩缩容平台。单台 Nginx、唯一主节点、共享数据库、共享存储和外部 AI/语音服务仍是明确的共享故障边界，不得宣传为全链路高可用。
