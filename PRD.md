# floating-ball-server 需求基线

## 病历模板管理统一口径

管理端“病历模板”统一以“门诊病例模板 / 住院病例模板”呈现。两侧列表、筛选、分页、病例预渲染和字段维护采用同一套操作语言；底层仍保持各自业务模型，不能为了界面一致把门诊模板对快照降级为住院 HTML 缓存。

门诊病例模板必须形成以下闭环：

1. 桌面端继续对 HIS 实际传入的渲染 HTML 与结构定义 JSON 做严格配对、动态字段解析和确定性自动映射；后台不接收患者、就诊、病例上下文、模型结果或医生编辑值。
2. 管理端以“病例预渲染 / 字段映射”作为主要操作。病例预渲染使用脱敏模拟值；字段映射展示自动映射来源、有效标准字段和投影方式，并允许管理员将字段改为指定标准字段或显式不映射。
3. 人工映射作为独立覆盖记录保存，不修改不可变的模板原文与解析快照。覆盖只对同一机构、同一模板版本生效；删除覆盖后恢复客户端原始自动映射。
4. 桌面端再次解析同一模板版本时，历史命中响应返回叠加人工覆盖后的有效字段映射；协议字段形状和既有映射来源枚举保持兼容。
5. 直接映射同一标准病例字段只能有一个字段；同一章节内的多个字段只有全部使用 `section-compose` 时才允许共同映射到同一标准字段。冲突保存必须失败，不得生成歧义回写。
6. HTML、结构定义 JSON 和完整解析 JSON 继续作为只读“解析详情”保留，不与主要病例查看流程混排。

## 萧山管理端辅诊功能专版

萧山只上线语音问诊、慢病配药、报告回诊、报告解读、医学助手 5 类业务功能。管理端“辅诊功能”入口使用萧山专版页面和专用后台接口，筛选项、指标、排行、趋势、明细与 Excel 导出都只能出现这 5 类。

为避免改变其他部署已有统计，原通用“辅诊功能”页面、`/admin/api/analytics/function-modules`、`/admin/api/analytics/function-usage` 与其导出接口必须保留原有 12 类功能事件口径；萧山专版使用独立的 `/admin/api/xiaoshan-analytics/*` 接口，不修改或复用通用接口的模块清单和统计查询。

萧山 5 类统计事实源如下：

1. 语音问诊、慢病配药、报告回诊、报告解读统一统计激活的 `voice`、`chronic_refill`、`report_consultation`、`report_interpretation` 问诊日志；一条问诊日志计一次调用，页面统计必须与用户日志按相同 HIS 机构、时间和场景透视后的结果一致。
2. 医学助手当前没有对应问诊日志，继续统计成功的 `chat` 功能事件；这是萧山专版唯一保留的功能事件事实源。
3. 两类事实统一应用时间、区域、后台机构和 HIS 机构筛选；不读取 `c_ai_op_log`，避免底层 AI/审计步骤被重复计为业务调用。
4. 功能使用率固定以 5 类已上线功能为分母；使用医生数优先按医生 ID 去重，医生 ID 为空时回退设备 ID。

## 管理端用户活跃度医生口径

用户活跃度统一按问诊日志中的医生 ID 统计，不再按设备统计。同一医生使用多台设备时只计 1 人；缺少医生 ID 的旧日志不进入医生人数。筛选范围内历史上出现过的医生构成总医生集合，所选时段有问诊的计为活跃医生，无问诊的计为不活跃医生。汇总、区域统计、明细列表和 Excel 导出必须使用相同口径。

## 两慢病插件一期

一期为 `floating-ball` 医生桌面端提供高血压与 2 型糖尿病慢病随访持久化，不包含患者端，也不建设管理端规则配置页。

边界如下：

1. 慢病保存请求严格采用对接系统原始 `TcdVisitForm` 字段和结构；不得用平台自创的英文通用字段替换原字段。
2. 服务端接收并保存医生确认后的融合随访记录；`sdVisitKind=1/2/1,2` 分别表示高血压、糖尿病、联合随访。公卫身份由桌面端上游 `rqflStatus` 派生，页面只读，不允许医生在表单内增删病种。
3. 设备鉴权使用现有 Bearer device token 与 ECDSA P-256 请求签名，不新增绕过路径。
4. AI 不是规则发布者。AI 只生成医生确认前的健康处方草稿，不修改正式路径或历史随访记录。
5. 一期管理端无新增页面；模板与路径使用代码仓库审核清单和数据库基线发布。
6. 健康处方和年度评估打印前保存强类型快照，至少包含患者/就诊锚点、病种、数据截至时间、规则/路径版本、年度指标、医生确认项与打印医生；快照保存失败不得被客户端视为可追溯打印完成。
7. 单病种和双病种都保存一条融合记录；原实例 8 个多选数组在提交前转成逗号字符串，`drugList` 保持对象数组。稳定幂等键使用签名请求头 `X-Request-Id`，同一机构内重试不得形成重复记录。
