# floating-ball-server 需求基线

## 两慢病插件一期

一期为 `floating-ball` 医生桌面端提供高血压与 2 型糖尿病慢病随访持久化，不包含患者端，也不建设管理端规则配置页。

边界如下：

1. 慢病保存请求严格采用对接系统原始 `TcdVisitForm` 字段和结构；不得用平台自创的英文通用字段替换原字段。
2. 服务端接收并保存医生确认后的融合随访记录；`sdVisitKind=1/2/1,2` 分别表示高血压、糖尿病、联合随访。公卫身份由桌面端上游 `rqflStatus` 派生，页面只读，不允许医生在表单内增删病种。
3. 设备鉴权使用现有 Bearer device token 与 ECDSA P-256 请求签名，不新增绕过路径。
4. AI 不是规则发布者。AI 只生成医生确认前的健康处方草稿，不修改正式路径或历史随访记录。
5. 一期管理端无新增页面；模板与路径使用代码仓库审核清单和数据库基线发布。
6. 健康处方和年度评估打印前保存强类型快照，至少包含患者/就诊锚点、病种、数据截至时间、规则/路径版本、年度指标、医生确认项与打印医生；快照保存失败不得被客户端视为可追溯打印完成。
7. 单病种和双病种都保存一条融合记录；原实例 8 个多选数组在提交前转成逗号字符串，`drugList` 保持对象数组。稳定幂等键使用签名请求头 `X-Request-Id`，同一机构内重试不得形成重复记录。
